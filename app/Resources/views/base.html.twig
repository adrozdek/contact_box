<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}{% endblock %}</title>
        {% block stylesheets %}
            {#<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">#}
            <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">
            <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
            <link href="{{ asset('css/mystyle.css') }}" rel="stylesheet">
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />

    </head>
    <body>

    <div class="container">
        {% block access %}
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                Zalogowany jako: {{ app.user.username | capitalize }}
                <a href="{{ path('fos_user_security_logout') }}">
                    Wyloguj
                </a>
            {% else %}
                <a href="{{ path('fos_user_security_login') }}">Logowanie</a>
            {% endif %}
        {% endblock %}

        {% block body %}{% block all %}{% block edit %}{% endblock %}{% endblock %}{% endblock %}
        {% block javascripts %}{% endblock %}
        {% block footer %}


                <hr>
                <a href="{{ path('showAll') }}"><b>WSZYTKIE KONTAKTY</b></a> &nbsp;
                <a href="{{ path('showAllGroups') }}"><b>WSZYSTKIEG GRUPY</b></a>


        {% endblock %}
        </div>
    </body>
</html>
